# SpeedMeter — Internet Speed Meter
# Core
speedtest-cli>=2.1.3
rich>=13.7.0
textual>=0.52.0
psutil>=5.9.8
platformdirs>=4.2.0

# Optional — desktop widget (tkinter is usually bundled with Python)

# Development
pytest>=8.0
pytest-asyncio>=0.23.0
ruff>=0.3.0